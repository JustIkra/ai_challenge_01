---
description: Execute command in container
argument-hint: <service> <command...>
---

Execute command in a container.
Use MCP tool `compose_exec` from docker-runner server.
Parameters: service=$1, command=$ARGUMENTS (everything after service name).
