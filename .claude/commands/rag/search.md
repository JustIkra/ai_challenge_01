---
description: Semantic search in project
argument-hint: <query> [limit]
---

Search for relevant code and documentation using semantic similarity.
Use MCP tool `rag_search` from rag-server.
Parameters: query=$1, limit=$2 (default 5).
Display results with file paths, similarity scores, and content snippets.
