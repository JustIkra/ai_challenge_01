# Project Assistant Bot — Design Document

**Дата:** 2026-01-16
**Статус:** Draft → Review

---

## 1. Обзор и назначение

**Project Assistant Bot** — персональный Telegram-бот для соло-разработчика, который:
- Отвечает на вопросы о проекте через RAG-поиск по всему репозиторию
- Создаёт задачи в MCP Reminder через естественный язык
- Даёт рекомендации по приоритетам на основе анализа текущих задач
- Показывает статус Docker-контейнеров

Архитектурно это **ReAct-агент** с собственной реализацией (без фреймворков), который принимает решения о вызове инструментов на основе контекста запроса.

| Параметр | Значение |
|----------|----------|
| Целевая аудитория | Соло-разработчик (только вы) |
| Авторизация | Отсутствует (бот не публичный) |
| Состояние | Stateless (без памяти между сообщениями) |
| LLM | Конфигурируемо через .env (OpenRouter) |
| Характер | Нейтральный/деловой |

---

## 2. Инструменты агента

Агент имеет доступ к трём инструментам:

| Инструмент | Описание | Интеграция |
|------------|----------|------------|
| `rag_search` | Семантический поиск по репозиторию | HTTP API (rag-server:8801) |
| `reminder_*` | Управление задачами (add, list, complete, summary) | HTTP API (reminder-server:8802) |
| `docker_status` | Статус контейнеров | Прямой вызов `docker compose ps` |

### Сценарии использования

1. **"Добавь задачу на завтра: фикс авторизации"** → `reminder_add`
2. **"Что сейчас в работе?"** → `reminder_list` + `reminder_summary`
3. **"Как работает RAG в проекте?"** → `rag_search`
4. **"Работают ли сервисы?"** → `docker_status`
5. **"Что делать в первую очередь?"** → `reminder_list` → LLM анализ → рекомендации

---

## 3. Архитектура

```
┌─────────────────┐
│  Telegram Bot   │
│   (aiogram)     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  ReAct Agent    │
│  (custom loop)  │
└────────┬────────┘
         │
    ┌────┴────┬──────────────┐
    ▼         ▼              ▼
┌───────┐ ┌────────┐  ┌────────────┐
│  RAG  │ │Reminder│  │Docker CLI  │
│ HTTP  │ │ HTTP   │  │(subprocess)│
└───────┘ └────────┘  └────────────┘
```

### Компоненты

1. **project-assistant/** — новый Python-сервис
   - `main.py` — aiogram bot с long polling
   - `agent/` — ReAct агент
     - `executor.py` — основной loop
     - `tools.py` — определения инструментов
   - `clients/` — HTTP клиенты
     - `rag.py` — для rag-server
     - `reminder.py` — для reminder-server
     - `docker.py` — обёртка над docker compose
   - `llm/` — OpenRouter клиент
   - `config.py` — конфигурация из .env

2. **mcp/reminder/** — расширение
   - Добавить HTTP API (аналогично rag-server)

3. **mcp/rag-server/** — без изменений (HTTP API уже планируется)

---

## 4. ReAct Agent Flow

```
User message
     │
     ▼
┌────────────────────────────────────┐
│ 1. Send "typing" action            │
└────────────────┬───────────────────┘
                 │
                 ▼
┌────────────────────────────────────┐
│ 2. Build initial prompt:           │
│    - System prompt (persona)       │
│    - Available tools (JSON schema) │
│    - User question                 │
└────────────────┬───────────────────┘
                 │
                 ▼
┌────────────────────────────────────┐
│ 3. Call LLM with function calling  │
└────────────────┬───────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
   [tool_call]       [final_answer]
        │                 │
        ▼                 ▼
┌───────────────┐   ┌──────────────┐
│ Execute tool  │   │ Return to    │
│ Add result    │   │ user         │
│ to context    │   └──────────────┘
└───────┬───────┘
        │
        ▼ (loop back to step 3)
```

### Ограничения

- **Timeout:** 30 секунд на весь запрос
- **Tool calls:** Без явного лимита (LLM решает)
- **При ошибке инструмента:** Отказ с сообщением

### Формат tool result

```json
{
  "tool": "rag_search",
  "success": true,
  "result": { ... }
}
```

При ошибке:
```json
{
  "tool": "rag_search",
  "success": false,
  "error": "Service unavailable"
}
```

---

## 5. API Contracts

### RAG Server HTTP API

**POST /api/search**
```json
// Request
{ "query": "как работает авторизация", "limit": 5 }

// Response
{
  "results": [
    {
      "file_path": "backend/app/services/auth.py",
      "content": "...",
      "similarity": 0.89
    }
  ]
}
```

### Reminder HTTP API (новый)

**GET /api/list**
```json
// Response
{
  "reminders": [
    {
      "id": "abc123",
      "text": "Фикс авторизации",
      "due_date": "2026-01-17",
      "completed": false
    }
  ]
}
```

**POST /api/add**
```json
// Request
{ "text": "Фикс авторизации", "due_date": "2026-01-17" }

// Response
{ "id": "abc123", "text": "Фикс авторизации" }
```

**POST /api/complete**
```json
// Request
{ "id": "abc123" }

// Response
{ "success": true }
```

**GET /api/summary**
```json
// Response
{ "active": 5, "overdue": 2, "completed_today": 1 }
```

### Docker Status

Прямой вызов:
```bash
docker compose ps --format json
```

---

## 6. Telegram Bot UX

### Команды

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие + список возможностей |
| `/help` | Подробная справка с примерами |
| `/status` | Краткая сводка: задачи + Docker статус |

### Формат ответов

**На вопрос о проекте (RAG):**
```
Авторизация реализована через JWT токены в backend/app/services/auth.py.

Основные компоненты:
1. Генерация токена при логине
2. Middleware для валидации
3. Refresh token механизм

📚 Источники: auth.py, middleware.py
```

**На создание задачи:**
```
✅ Задача создана: "Фикс авторизации"
Дедлайн: завтра (17 января)
```

**На запрос статуса:**
```
📋 Задачи:
• Активных: 3
• Просроченных: 1

🐳 Docker:
• backend: running
• frontend: running
• postgres: running
```

**На рекомендацию по приоритетам:**
```
Рекомендую начать с:

1. "Фикс авторизации" — просрочено на 2 дня, критичный функционал
2. "Добавить тесты" — блокирует деплой
3. "Рефакторинг API" — низкий приоритет, можно отложить
```

### Edge Cases

| Ситуация | Поведение |
|----------|-----------|
| Непонятный запрос | "Не понял запрос. Уточните, что вы хотите сделать?" |
| RAG не нашёл | "Не нашёл информации по этому вопросу. Попробуйте переформулировать?" |
| Инструмент недоступен | "Ошибка: сервис X недоступен. Попробуйте позже." |
| Неподдерживаемое действие | "Это вне моих возможностей. Доступны: поиск по проекту, управление задачами, статус Docker." |
| Стикер/фото/голос | Игнорировать |
| Сообщение в группе | Игнорировать |

---

## 7. Конфигурация и Deployment

### .env переменные

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=<token>

# OpenRouter
OPENROUTER_API_KEYS=<key1,key2>
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=anthropic/claude-3-haiku

# Services
RAG_SERVER_URL=http://rag-server:8801
REMINDER_SERVER_URL=http://reminder-server:8802

# Limits
AGENT_TIMEOUT=30
MAX_INPUT_LENGTH=1000
MAX_OUTPUT_LENGTH=4000
```

### docker-compose.yml (дополнение)

```yaml
project-assistant:
  build:
    context: ./project-assistant
    dockerfile: Dockerfile
  environment:
    - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    - OPENROUTER_API_KEYS=${OPENROUTER_API_KEYS}
    - OPENROUTER_BASE_URL=${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}
    - OPENROUTER_MODEL=${OPENROUTER_MODEL:-anthropic/claude-3-haiku}
    - RAG_SERVER_URL=http://rag-server:8801
    - REMINDER_SERVER_URL=http://reminder-server:8802
    - AGENT_TIMEOUT=${AGENT_TIMEOUT:-30}
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock:ro  # для docker status
  depends_on:
    - rag-server
    - reminder-server
  restart: unless-stopped

reminder-server:
  build:
    context: ./mcp/reminder
    dockerfile: Dockerfile
  ports:
    - "8802:8802"
  volumes:
    - reminder-data:/app/data
  restart: unless-stopped
```

### Структура файлов

```
project-assistant/
├── Dockerfile
├── requirements.txt
├── src/
│   ├── __init__.py
│   ├── main.py           # Bot entry point
│   ├── config.py         # Environment config
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── commands.py   # /start, /help, /status
│   │   └── messages.py   # Text message handler
│   ├── agent/
│   │   ├── __init__.py
│   │   ├── executor.py   # ReAct loop
│   │   ├── tools.py      # Tool definitions
│   │   └── prompts.py    # System prompts
│   ├── clients/
│   │   ├── __init__.py
│   │   ├── rag.py        # RAG HTTP client
│   │   ├── reminder.py   # Reminder HTTP client
│   │   └── docker.py     # Docker status client
│   └── llm/
│       ├── __init__.py
│       └── openrouter.py # OpenRouter client

mcp/reminder/
├── Dockerfile            # NEW
├── http-server.mjs       # NEW: HTTP API wrapper
└── index.mjs             # Existing MCP server
```

---

## 8. Acceptance Criteria

- [ ] Агент отвечает на вопрос о проекте с источниками из RAG
- [ ] Агент создаёт задачу через естественный язык ("добавь задачу X")
- [ ] Агент показывает список задач и сводку
- [ ] Агент показывает статус Docker-контейнеров
- [ ] Агент даёт рекомендации по приоритетам на основе списка задач
- [ ] При недоступности инструмента — явная ошибка
- [ ] Timeout 30 секунд на запрос
- [ ] Команды /start, /help, /status работают
- [ ] Секреты не в git

---

## 9. Risks & Mitigations

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| LLM зацикливание | Средняя | Timeout 30 сек |
| Некорректный tool call | Низкая | Валидация параметров + try/catch |
| OpenRouter недоступен | Низкая | Явное сообщение об ошибке |
| RAG индекс устарел | Средняя | Документация: ручная переиндексация |
| Docker socket permission | Низкая | :ro mount + docs |

---

## 10. Out of Scope

- Авторизация пользователей
- Session memory (multi-turn conversations)
- Автоматические уведомления
- GitHub integration
- Inline keyboard buttons
- Автоматическая RAG переиндексация
- Юнит-тесты

---

## 11. Deliverables

1. **project-assistant/** — Python-сервис с aiogram + ReAct агент
2. **mcp/reminder/http-server.mjs** — HTTP API wrapper для reminder
3. **docker-compose.yml** — обновление с новыми сервисами
4. **README** в project-assistant/

---

## Appendix: System Prompt

```text
Ты ассистент разработчика. Помогаешь управлять проектом: отвечаешь на вопросы о коде, создаёшь задачи, показываешь статус.

Доступные инструменты:
- rag_search: поиск информации о проекте
- reminder_add: создание задачи
- reminder_list: список задач
- reminder_summary: сводка по задачам
- docker_status: статус контейнеров

Правила:
1. Отвечай кратко и по делу
2. Если инструмент нужен — используй его
3. Если не понял запрос — попроси уточнение
4. Если не можешь помочь — честно скажи
5. Показывай источники из RAG
6. Пиши на русском языке
```
