---
description: Start Docker Compose stack
argument-hint: [service]
---

Start the Docker Compose stack.
Use MCP tool `compose_up` from docker-runner server.
If service name provided ($1), start only that service.
After starting, show container status.
